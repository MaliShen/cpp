#ifndef SUBSTRING_H
#define SUBSTRING_H

using namespace std;

// function returns true if its first string argument is a prefix
// of its second string argument
bool is_prefix(char first[], char second[]);
  
int substring_position(char substring[], char string[]);

#endif
